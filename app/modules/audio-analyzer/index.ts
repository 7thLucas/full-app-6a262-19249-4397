// Client-safe re-exports from the audio-analyzer module.
// Keep in sync with src/index.ts — server-only imports (controller, routes, lib)
// must be imported directly from their deep paths.
export * from "./src/index";
